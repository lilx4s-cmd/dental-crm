# Railway/Nixpacks build config for the NestJS API
providers = ["node"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build --workspace=packages/shared",
  "npm run build --workspace=apps/api"
]

[start]
cmd = "node apps/api/dist/main.js"
